# Config model

Moved. How one `.app` self-tunes - the per-arch baseline, the `hw.model`
overlay, the deliberate absence of an iMac G4 profile, and which OS each slice
needs - is now `docs/adr/0007-one-app-self-tunes-per-arch-and-per-machine.md`
and `docs/adr/0002-three-slices-floored-at-the-oldest-os-each-cpu-can-run.md`.

This stub exists because `code/qcommon/common.c` cites this path.
